mutation RequestReviews($pull_request_id: String!, $users: [String!]) {
  requestReviewsByLogin(input: {pull_request_id: $pull_request_id, union: true, userLogins: $users}) {
    clientMutationId
  }
}
